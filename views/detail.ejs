<!Doctype html>
<html>
<head>
  <meta charset="utf-8">
  <title>Show message</title>
</head>
<body>
  <h2>詳細</h2>
  <table>
      <% for( let row of data ) { %>
        <tr>
          　<td><%= row.data_id %></td>
            <td><%= row.map_id %></td>
            <td><%= row.map2_id %></td>
            <td><%= row.treasure_id %></td>
        </tr>
      <% } %>
  </table>
</body>
</html>